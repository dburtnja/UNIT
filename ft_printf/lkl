
#include <stdio.h>
int check_size(char *str, int *i);

int main(void)
{
	char *str = "++--01hh0l";
	int	i;
	int	b;
	int	b_old;

	b_old = 0;
	i = 0;
	while (str[i] != 0)
	{
		if ((b = check_size(str, &i)) > b_old)
			b_old = b;
		i++;
	}
	printf("b_old = %d\n", b_old);
	return (0);
}
